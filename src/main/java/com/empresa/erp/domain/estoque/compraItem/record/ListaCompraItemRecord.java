package com.empresa.erp.domain.estoque.compraItem.record;
/*
import java.math.BigDecimal;

import com.empresa.erp.domain.estoque.compraItem.model.CompraItemModel;

public record ListaCompraItemRecord(				
		Long id,
		String fornecedor,
		String produto,
		Integer quantidade,
		BigDecimal valor,
		BigDecimal total
) {
	
	public ListaCompraItemRecord(CompraItemModel dados) {
		this(
				dados.getId(),
				dados.getFornecedor().getRazaoSocial(),
				dados.getProduto().getNome(),
				dados.getQuantidade(),
				dados.getValor(),
				dados.getTotal()
		);
	}	
	
}
*/